// Пример 1: Извлечение всех чисел из строки
const text = "В 2023 году было 456 событий, и 78 из них были важными.";
const numbers = text.match(/\d+/g);
console.log(`Извлечённые числа: ${numbers}`);

